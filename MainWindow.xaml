<Window x:Class="SessionProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SessionProject"
        mc:Ignorable="d"
        Title="Проект по сессии" Height="600" Width="1000"
        Style="{StaticResource CommonStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5">
            <Image Source="/Resources/logo.png" Width="30" HorizontalAlignment="Left" Margin="0,0,5,0"/>
            <TextBlock Text="Название компании" VerticalAlignment="Center"/>
        </StackPanel>

        <WrapPanel Grid.Row="1">
            
        </WrapPanel>
        
        <Frame x:Name="NavigationFrame" Grid.Row="2"/>
    </Grid>
</Window>
